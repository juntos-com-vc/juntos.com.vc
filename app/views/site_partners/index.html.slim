#page_content
  .container
    .site-partners
      h1 Parceiros
      h2 Conheça nossos principais parceiros
      
      .site-partners-nav
        - @site_partners.each do |partner|
          .partner
            = image_tag partner.logo.regular.url


      .partners-details
        - @site_partners.each do |partner|
          .partner-details
            h3 = partner.name
            h4
              span Endereço:
              =< partner.address
            h4
              span Contato:
              =< partner.phone
            h4
              span Site:
              =< link_to partner.url
            p
              = partner.bio

      = content_for :custom_javascripts do
        javascript:
          $(document).ready(function(){
            $('.partners-details').slick({
              slidesToShow: 1,
              slidesToScroll: 1,
              arrows: false,
              fade: true,
              asNavFor: '.site-partners-nav'
            });
            $('.site-partners-nav').slick({
                slidesToShow: 5,
                slidesToScroll: 1,
                asNavFor: '.partners-details',
                centerMode: true,
                focusOnSelect: true,
                prevArrow: '<button type="button" class="slick-prev custom-arrow"></button>',
                nextArrow: '<button type="button" class="slick-next custom-arrow"></button>'
            });
          });
          


